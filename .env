# API Mode: set to 'true' to use backend API, 'false' for localStorage
VITE_USE_API=true

# Backend API URL (only used when VITE_USE_API=true)
# Leave empty for Docker (uses nginx proxy), set to http://localhost:8080 for local dev
VITE_API_URL=
